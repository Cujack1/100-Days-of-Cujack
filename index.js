document.getElementById("upload-btn").addEventListener("click", displayText);

const files = document.getElementById("file-upload").file[0];

function displayText() {
    let reader = new FileReader();

}